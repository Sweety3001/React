state-> Js
UI-> DOM
react is used to have a connection between JS and DOM
React is Library

React
->react-DOM(web apps)
->react-native(mobile apps)

npm create vite@latest

jsx can only return one element

Virtual DOM
compares browser tree and it's own js tree
makes a tree like structure and changes only what need
do not change the whole page as browser does.



